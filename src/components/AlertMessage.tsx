

export default function AlertMessage() {

  return (
    <div className="sticky top-0 z-50">
      <div
        role="alert"
        className="alert alert-info rounded-none flex items-center justify-center text-center">
        <span>🚧 La web está en construcción. ¡Pronto novedades! 🚧</span>
      </div>
    </div>
  );
}
